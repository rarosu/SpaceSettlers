/**
    Used for every entity that is possible to render.
*/
define(function(require) {
    "use strict";
    
    return {
        mesh: null,
        addedToScene: false, 
        removeFromScene: false
    };
});